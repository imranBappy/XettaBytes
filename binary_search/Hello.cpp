// p ∗ e
// −x + q ∗ sin(x) + r ∗ cos(x) + s ∗ tan(x) + t ∗ x 2 + u = 0
//

#include <bits/stdc++.h>
using namespace std;
int main()
{
    // −x + q ∗ sin(x) + r ∗ cos(x) + s ∗ tan(x) + t ∗ x 2 + u = 0
    // 0 0 0 0 -2 1
    // int x =

    /*
        p ∗ e^x + q ∗ sin(x) + r ∗ cos(x) + s ∗ tan(x) + t ∗ x^2 + u = 0
    */
    //    0 0 0 0 -2 1
    // int x = 0 * pow(0,0) + 0 sin
    cout << "Hello World!" << endl;

    return 0;
}